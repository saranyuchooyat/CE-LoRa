version: '3.8'

services:
  backend:
    build:
      context: ./BackEnd
      dockerfile: Dockerfile
    ports:
      - "8081:8080"
    environment:
      - MONGO_URI=mongodb://admin_kmitl:kmitl123@100.118.210.62:27017/LoRa?authSource=LoRa
      - JWT_SECRET=xx12345ce
    restart: always

  frontend:
    build:
      context: ./FrontEnd
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=https://ana02.tailf85900.ts.net:8443
    ports:
      - "3000:80"
    restart: always